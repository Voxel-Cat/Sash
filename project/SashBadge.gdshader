shader_type canvas_item;

void vertex() {
	// Called for every vertex the material is visible on.
}
void fragment() {

uniform vec4 target_color : source_color = vec4(0.278,0.549,0.7,1.0);
uniform vec4 replacement_color : source_color = vec4(1,1,1,1.0);
uniform float tolerance = 0.5;

void fragment() {
	vec4 curr
}
